## 关于此项目

### **SealVault**

**SealVault** 是一个专注于 **Windows 平台** 的安全文件/文件夹加密工具，

它通过 **强加密算法** 与 **Windows Explorer 深度集成**，

为用户提供一种 **直观、可靠、不可被预览或旁路访问** 的数据保护方式。

SealVault 的设计目标并不是“把文件打包成压缩包”，

而是让被加密的文件在系统层面 **彻底不可识别**，

只有在用户输入正确密码后，才能通过 SealVault 进行解锁与访问。